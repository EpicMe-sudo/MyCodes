#include "fft.h"

void fft(uint32_t* polynomial,uint32_t* output,uint8_t degree){

    if (degree == 1){
        return (*output) = 
    }

    // divide step

    // recursive calls with omega^2 as n/2th root of unity

    // combine step, using x = omega^i

}